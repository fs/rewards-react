jest.mock('./ApiService');

describe('CommentService', () => {
  beforeEach(() => {
    jest.resetModules();
  });

  test('createComment HappyPath', async () => {
    // Arrange
    // Act
    // Assert
  });
});
